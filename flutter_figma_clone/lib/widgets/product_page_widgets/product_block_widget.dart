import 'package:flutter/material.dart';

class ProductBlockWidget extends StatelessWidget {
  const ProductBlockWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}